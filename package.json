{
  "name": "voice-claude",
  "version": "1.0.0",
  "description": "语音 → Claude Code · Electron + TypeScript",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && electron .",
    "dev": "tsc && node dev.js"
  },
  "dependencies": {
    "electron": "^34.5.8",
    "koffi": "^3.0.2",
    "vosk-browser": "^0.0.8"
  },
  "devDependencies": {
    "electron-reload": "^2.0.0-alpha.1",
    "typescript": "^5.5.0"
  }
}
